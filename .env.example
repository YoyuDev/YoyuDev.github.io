# 环境变量示例。
# 需要用到时复制成 .env.local（该文件已被 .gitignore 忽略，不会提交到仓库）。

# 站点完整地址，用于生成 canonical 与 og:url。
# 不填也能正常运行，只是不会输出 canonical 链接。
# 部署到 GitHub Pages 项目站点时示例：
# VITE_SITE_URL=https://yoyudev.github.io
# 绑定自定义域名后示例：
# VITE_SITE_URL=https://your-domain.com
VITE_SITE_URL=

# 部署子路径。本地开发不要设置（默认就是 '/'）。
# GitHub Actions 会自动计算并注入，不需要手动配置。
# 手动构建到项目站点时可以用：
# VITE_BASE_PATH=/yizhi-portfolio/
VITE_BASE_PATH=
