/**
 * 站点计数（访问量 / 点赞）的配置。
 *
 * ⚠️ 这是个纯静态站（GitHub Pages，没有后端），数字只能存在第三方服务上。
 *    这里用 Abacus：**免注册、免 key**，支持自增和读取，一个服务同时管访问量和点赞。
 *    实测的代价（2026-09-17）：
 *      - 国内延迟约 350–500ms 稳定，首次建连 1.6s —— 异步拉取，不挡首屏；
 *      - **设不了初始值**（`hit?amount=` 和 `/set` 都不支持，会静默忽略），只能从 1 往上加；
 *      - 数据存在第三方，没有后台看明细，也没有防刷。
 *    哪天想换成自建（Cloudflare Workers + KV）或者换服务商，**只动这个文件的
 *    endpoint 和路径拼法**即可，composable 和组件都不用碰。
 *
 * 改计数分区（比如换域名、想重新归零）时改 namespace 就行。
 */
export const SITE_STATS = {
  endpoint: 'https://abacus.jasoncameron.dev',
  /** 计数分区。同一个分区下用不同 key 区分「访问量」和「点赞」 */
  namespace: 'yizhi-portfolio',
  visitsKey: 'visits',
  likesKey: 'likes',

  /**
   * 访问量低于这个数就**不显示**（主人 2026-09-17 定的）。
   *
   * 新站刚上线，数字是个位数，访客（尤其面试官）看到「访问 4 次」只会觉得冷清，
   * 那还不如不显示 —— 攒过这条线它自己就出来了。
   *
   * 点赞**不做**这个限制：它是互动件，一个赞都没有的时候恰恰最需要摆出来让人去点。
   */
  visitsMinDisplay: 100,

  /** 计数请求超时（毫秒）。服务慢或挂掉时不能拖着导航栏干等 */
  timeout: 4000,
} as const
